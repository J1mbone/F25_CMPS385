#include <iostream>
#include <cmath>
using namespace std;

int main(){
    double r=4.7, h=10.9;
    double s, volume, surface;

    s = sqrt(r*r + h*h);
    volume = (1.0/3.0)*M_PI*r*r*h;
    surface = M_PI*r*r + M_PI*r*s;

    cout<<"Side = "<<s<<endl;
    cout<<"Volume = "<<volume<<endl;
    cout<<"Surface Area = "<<surface<<endl;
}
